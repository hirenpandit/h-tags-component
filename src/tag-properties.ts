export interface TagProperties {
  duplicate?: boolean
  label: string
  tagStyleClass?: string
}
export const DUPLICATE_MSG: string = 'Tag already exist'
export const EMPTY_MSG: string = 'Tag is empty'
